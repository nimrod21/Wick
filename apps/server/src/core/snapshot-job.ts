/**
 * Snapshot job — STUB.
 *
 * Phase 4 will schedule forward-price snapshots for recorded events
 * (horizons: 5m, 30m, 1h, 4h, 1d, 3d, 1w, 3w). Phase 7 uses these rows to
 * back-test signal strength.
 */

export function startSnapshotJob(): never {
  throw new Error('snapshot-job: implement in Phase 4');
}
