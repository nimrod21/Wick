/**
 * Probability engine — STUB.
 *
 * Phase 7 will implement the Bayesian/weighted signal fusion producing
 * `probability_history` rows and the `PROB` indicator events.
 */

export function computeProbability(): never {
  throw new Error('probability-engine: implement in Phase 7');
}
