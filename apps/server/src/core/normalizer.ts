/**
 * Event normalizer — STUB.
 *
 * Phase 2 will implement the canonical mapping from collector-specific shapes
 * (Etherscan / Helius / CryptoPanic / Binance / Alpaca / ...) into the unified
 * `Event` union defined in `@cockpit/shared/events`.
 */

export function normalize(): never {
  throw new Error('normalizer: implement in Phase 2');
}
