/**
 * Alert engine — STUB.
 *
 * Phase 8 will evaluate `alert_rules`, apply cooldowns, record `alert_firings`,
 * and fan out to configured channels (desktop, telegram, SSE `alert` events).
 */

export function evaluateAlerts(): never {
  throw new Error('alert-engine: implement in Phase 8');
}
